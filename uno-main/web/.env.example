# Frontend environment variables for Vite
# Map to your ngrok tunnels:
# - Frontend (Vite dev) is served at: https://09415bce9f13.ngrok-free.app -> http://localhost:3000
# - Backend (Flask-SocketIO) is served at: https://e2d8a71d41b4.ngrok-free.app -> http://localhost:4000

# HTTP base for backend REST API. Use the backend ngrok URL.
VITE_SERVER_HTTP_URL=https://e2d8a71d41b4.ngrok-free.app

# WebSocket base for Socket.IO. Use wss with the backend ngrok host.
VITE_SERVER_WS_URL=wss://e2d8a71d41b4.ngrok-free.app

# Note: You can also override at runtime with query params ?api=...&ws=...
